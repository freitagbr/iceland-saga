export const likeNull = x => x == null;
export const isNumber = x => typeof x === 'number';
export const isString = x => typeof x === 'string';
export const isObject = x => typeof x === 'object';
export const isUndefined = x => typeof x === 'undefined';
